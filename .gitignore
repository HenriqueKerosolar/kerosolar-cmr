# dependencies
/node_modules

# next.js
/.next/
/out/
/build
tsconfig.tsbuildinfo

# env / segredos (NUNCA versionar)
.env
.env.local
.env*.local
# mantém o exemplo
!.env.example

# WhatsApp (Baileys) — sessões/credenciais sensíveis
/wa-sessions/

# Anexos enviados pelo chat (em dev ficam aqui; em prod vão para /data)
/uploads/

# Scripts temporários de diagnóstico
/*.tmp.mjs

# Materiais de marketing / geradores (não versionar)
/*.pptx
/gerar-apresentacao.cjs

# logs e SO
*.log
npm-debug.log*
.DS_Store
Thumbs.db

.vercel
